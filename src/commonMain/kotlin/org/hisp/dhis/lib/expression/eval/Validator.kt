package org.hisp.dhis.lib.expression.eval

class Validator { // Rule engine
    // parses
    // arguments have right type (can be converted)
    // variables should exist (set of names provided)
    // no data item in the AST (C is allowed)
}
